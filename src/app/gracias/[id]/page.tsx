export default async function GraciasPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  return <main className="event-shell grid place-items-center text-center"><section className="event-panel w-full max-w-md border-t-8 border-event-yellow"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-event-yellow text-3xl font-black">✓</div><p className="event-kicker mt-6">Pago registrado</p><h1 className="event-title mt-2">Gracias por tu compra</h1><p className="mt-4 text-neutral-600">Te enviamos un correo con el resumen de tu compra. Revisaremos el comprobante y, cuando confirmemos el pago, recibirás un segundo correo con tus entradas QR.</p><div className="mt-6 bg-cream p-4 text-left"><span className="event-label">Código de registro</span><p className="break-all font-mono text-xs font-bold">{id}</p></div><Link href="/" className="event-button-dark mt-6 w-full">Volver al inicio</Link></section></main>;
}
import Link from "next/link";
